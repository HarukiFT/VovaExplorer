import Header from "./Header"
import Styles from '../styles/App.module.scss'

export default () => {
    return (
        <div className={Styles.wrapper}>
            <Header />
        </div>
    )
}